# Contributing

Keep the module simple, readable and compatible with AzerothCore.

Rules:

- keep configuration clear,
- avoid unsafe player/account changes,
- avoid external dependencies,
- document SQL changes,
- keep custom IDs configurable.
